apiVersion: v1
kind: Service
metadata:
  name: wisdompet-service
  namespace: wisdompet-namespace
  labels:
    app: wisdompet-app
spec:
  type: LoadBalancer
  selector:
    app: wisdompet-app
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: wisdompet-deployment
  namespace: wisdompet-namespace
  labels:
    app: my-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: wisdompet-app
  template:
    metadata:
      labels:
        app: wisdompet-app
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: beta.kubernetes.io/arch
                operator: In
                values:
                - amd64
                - arm64
      containers:
      - name: wisdompet
        image: lingxueli/wisdompet:latest-v2
        ports:
        - containerPort: 8000
